function isOldEnoughToDrive(age) {
  // your code here
if (age >= 16) {
        return true;
    } else if (age < 16) {
        return false;
    }
} 

var output= isOldEnoughToDrive(16);
console.log(output);

