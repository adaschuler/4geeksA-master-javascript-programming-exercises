function isOldEnoughToDrinkAndDrive(age) {
  // your code here
    if (age >= 21) {
        return false;
    } else if (age < 21) {
        return false;
    }
}

var output=isOldEnoughToDrinkAndDrive(16);
console.log(output);

